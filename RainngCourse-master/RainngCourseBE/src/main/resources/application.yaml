server:
  port: 8085

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver #数据库驱动
    url: jdbc:mysql://localhost:3306/rainng_course?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC  # 数据库的 JDBC URL
    username: root
    password: 123456
  jackson:
    time-zone: Asia/Shanghai
mybatis-plus:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.rainng.coursesystem.model